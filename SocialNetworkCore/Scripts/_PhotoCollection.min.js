function LoadDetailsBegin() { var t = $("div#prg_photo_collection"), e = t.parent().width(), i = t.parent().height(), n = e / 2 - t.outerWidth() / 2, o = i / 2 - t.outerHeight() / 2; t.css("left", n + "px").css("top", o + "px").show() } $(function () { $("a.next, a.prev").live("click", function () { change_address_bar($(this).next("input").val()) }) });